using UnityEngine;

public class VehicleTrafficManager : MonoBehaviour
{
    public GameObject[] TrafficSpawnPoints;

    // Use this for initialization
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
}